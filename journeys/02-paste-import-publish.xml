<journey name="Paste-to-Import — parse, publish, undo window">
  <description>
    The v1 primary import flow (spec §5): paste tab-separated lines, preview parsed cards,
    publish to the homeserver, observe the 10-second undo window, finish to deck detail.
    Requires a signed-in session (run journey 01 first).
  </description>
  <actions>
    <action>Launch the Loopky app and verify the Home screen is shown</action>
    <action>Tap the element with resource-id "tab_decks" in the bottom tab bar</action>
    <action>Tap the element with resource-id "decks_paste_cta"</action>
    <action>Verify the "Paste cards" screen is shown with a text area placeholder "Paste your list here" (resource-id: paste_input)</action>
    <action>Type a single word "dog" (no front/back separator) into the field with resource-id "paste_input"</action>
    <action>Verify no card preview is shown and the summary chip reads "No pattern detected" (it only becomes a "Detected" chip once a front/back separator is present)</action>
    <action>Verify the card count reads "1 card" and not "1 cards" (regression: the app had no plural resources)</action>
    <action>Clear the field with resource-id "paste_input"</action>
    <action>Type the following three lines into the field with resource-id "paste_input": "dog,cachorro", "cat,gato", "bird,passaro"</action>
    <action>Verify the summary chip now reads "Detected: comma" and a live preview appears showing 3 parsed cards with fronts dog, cat, bird</action>
    <action>Tap the element with resource-id "paste_next"</action>
    <action>Verify the "Review cards" triage screen is shown with a card (resource-id: triage_card), a progress label "1 of 3" (resource-id: triage_progress), and Discard/Edit/Keep action buttons</action>
    <action>Tap the element with resource-id "triage_keep" to keep the first card</action>
    <action>Tap the element with resource-id "triage_keep" to keep the second card</action>
    <action>Tap the element with resource-id "triage_keep" to keep the third (last) card and advance to Publish</action>
    <action>Verify the "New deck" publish screen is shown with a "Card Options" section</action>
    <action>Verify the Listen toggle (resource-id: publish_listen_toggle) and Speak toggle (resource-id: publish_speak_toggle) are both OFF by default — turning either on obliges the author to declare the deck's language pair, so neither may default on</action>
    <action>Enter "Animals PT" into the field with resource-id "publish_title"</action>
    <action>Tap the element with resource-id "publish_button"</action>
    <action>Verify a "Deck published!" success state appears with an Undo button showing a live countdown (resource-id: publish_undo) and a Done button (resource-id: publish_done)</action>
    <action>Tap the element with resource-id "publish_done"</action>
    <action>Verify the deck detail screen for "Animals PT" is shown with 3 cards</action>
    <action>Tap the element with resource-id "deck_detail_back"</action>
    <action>Tap the element with resource-id "tab_decks"</action>
    <action>Verify the Decks grid lists "Animals PT" WITHOUT restarting the app (regression: publishing did not invalidate the deck list, so this said "No decks yet" until the process restarted)</action>
  </actions>
</journey>
